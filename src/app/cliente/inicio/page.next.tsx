import { ShellPlaceholder } from "@/components/shells/shell-placeholder.next";

export default function ClienteInicioPage() {
  return (
    <ShellPlaceholder
      eyebrow="Cliente"
      title="Shell Cliente"
      description="Estrutura técnica do perfil Cliente em /cliente. O portal atual em /patient continua preservado no Vite, sem redirects ou migração de conteúdo."
    />
  );
}
