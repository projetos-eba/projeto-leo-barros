import { ShellPlaceholder } from "@/components/shells/shell-placeholder.next";

export default function AdminDashboardPage() {
  return (
    <ShellPlaceholder
      eyebrow="Admin"
      title="Shell Admin"
      description="Estrutura técnica reservada à administração real da plataforma. Métricas, permissões, financeiro e demais regras ainda não foram definidos nem implementados."
    />
  );
}
